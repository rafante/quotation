ID: quotation
version: 1.0.0
_schema-version: 2.0.0

parameters:
  hcp-deployer-version: 1.2.0

modules:
  - name: quotation
    type: html5
    path: .
    parameters:
      display-name: quotation
      version: ${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist
